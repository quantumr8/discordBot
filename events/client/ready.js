//const guildID = '800539900499066920'

module.exports = () => {
    console.log("Bot is online!");

    /*const commands = await clientInformation.appVersion
        .applications(client.user.id)
        .guilds(guildID)
        .commands.get()
    console.log(commands)
    */
}